use crate::{lexer::{SpannedToken, Token}, ast::Item};

pub fn parse_item<'source>(tokens: &mut impl Iterator<Item = SpannedToken<'source, Token>>) {
	

}

pub fn parse_statement<'source>(tokens: &mut impl Iterator<Item = SpannedToken<'source, Token>>) {
	
}

pub fn parse_expression<'source>(tokens: &mut impl Iterator<Item = SpannedToken<'source, Token>>) {
	
}